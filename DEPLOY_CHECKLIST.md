# GitHub Pages 배포 체크리스트

## 업로드 전

- [ ] ZIP의 압축을 풀었다.
- [ ] `index.html`이 프로젝트 최상위에 있다.
- [ ] `styles.css`, `.nojekyll`, `js` 폴더가 `index.html`과 같은 위치에 있다.
- [ ] 이전 버전 파일과 섞지 않고 저장소 내용을 새 버전으로 교체한다.

## GitHub 업로드

- [ ] 저장소 첫 화면에서 `index.html`이 바로 보인다.
- [ ] `js/app.js`와 `js/camera-controller.js`가 업로드되어 있다.
- [ ] 커밋이 `main` 브랜치에 들어갔다.

## Pages 설정

- [ ] `Settings → Pages`로 이동했다.
- [ ] Source: `Deploy from a branch`
- [ ] Branch: `main`
- [ ] Folder: `/(root)`
- [ ] 저장 후 배포 완료 메시지를 확인했다.

## 배포 후 1분 점검

- [ ] 주소가 `https://사용자명.github.io/저장소명/` 형식이다.
- [ ] 제목과 세 입자 비교 화면이 정상적으로 보인다.
- [ ] `카메라 시작`을 누르면 카메라 권한 창이 나타난다.
- [ ] 권한 허용 후 카메라 영상이 보인다.
- [ ] 상단 `카메라`가 `연결됨`으로 바뀐다.
- [ ] 상단 `손 인식 방식`이 `MediaPipe 손 인식`, `MediaPipe 준비 중`, 또는 `움직임 감지(대체)` 중 하나로 표시된다.
- [ ] 손을 좌우로 빠르게 움직였을 때 풍향·바람 세기가 변한다.
- [ ] `수동 조작`에서 오른쪽 바람 6.0이 정상 작동한다.
- [ ] `한 종류 자세히`로 전환했을 때 큰 캔버스가 정상 표시된다.
- [ ] 모바일에서 문서 전체의 가로 스크롤이 생기지 않는다.

## 이상이 있을 때

1. `Command/Ctrl + Shift + R`로 강력 새로고침합니다.
2. 주소 끝에 `?v=21`을 붙여 다시 엽니다.
3. 학교망에서 MediaPipe가 막힌 경우 `?offline=1`을 붙여 카메라 움직임 감지를 사용합니다.
4. 카메라 권한이 계속 거부되면 사이트 설정에서 기존 권한을 삭제한 뒤 다시 허용합니다.
5. 카메라 앱·Zoom·Meet를 닫고 재시도합니다.
6. 마지막 대체 수단으로 `수동 조작`을 사용합니다.
