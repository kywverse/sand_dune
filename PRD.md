# 제품 요구사항 정의서(PRD)

## 1. 문서 정보

| 항목 | 내용 |
|---|---|
| 제품명 | 손으로 만드는 사구 실험실 |
| 버전 | 2.1.0 안정화 버전 |
| 대상 | 대한민국 중·고등학교 지구과학 수업의 교사와 학생 |
| 플랫폼 | 데스크톱·노트북·태블릿·스마트폰 웹브라우저 |
| 배포 | GitHub Pages 정적 웹사이트 |
| 핵심 기술 | MediaPipe Hand Landmarker, MediaDevices API, Canvas 2D, JavaScript ES Modules |
| 언어 | 학생 화면 전체 한국어 |

## 2. 배경과 문제 정의

학생은 사구가 바람에 의해 형성된다는 사실은 알고 있어도, 왜 모든 크기의 입자가 같은 방식으로 사구를 만들지 않는지는 직관적으로 이해하기 어렵다. 특히 다음과 같은 단순화가 오개념으로 이어질 수 있다.

- 입자가 작을수록 무조건 약한 바람에도 잘 날린다.
- 아주 작은 입자와 모래는 같은 방식으로 이동한다.
- 입자가 너무 크거나 작으면 어떤 조건에서도 절대로 퇴적되지 않는다.

본 제품은 학생이 손으로 바람을 조작하고 같은 바람을 서로 다른 입자 크기에 적용하여, `이동 시작 → 이동 방식 → 재퇴적 → 사구 성장`의 관계를 비교하도록 한다.

이전 버전에서는 카메라보다 MediaPipe 외부 파일을 먼저 불러와, CDN이 차단되면 카메라 자체도 시작되지 않는 문제가 있었다. 안정화 버전의 최우선 요구사항은 외부 손 인식 파일의 성공 여부와 카메라 시작을 분리하는 것이다.

## 3. 핵심 교육 질문

> 왜 사구는 아주 작은 입자나 큰 입자보다 모래 크기의 입자에서 잘 형성되는가?

학생이 활동 후 도달해야 할 핵심 설명은 다음과 같다.

> 사구는 바람에 의해 이동할 수 있으면서도 공중으로 너무 멀리 빠져나가지 않고 가까운 곳에 다시 쌓일 수 있는 크기의 모래에서 잘 형성된다.

## 4. 목표

1. 학생이 손 움직임으로 풍향과 상대 바람 세기를 조작한다.
2. 같은 바람에서 0.02 mm, 0.25 mm, 3.0 mm 입자의 차이를 동시에 비교한다.
3. 아주 작은 입자의 응집력과 공중 장거리 이동을 함께 설명한다.
4. 모래 크기 입자의 이동과 재퇴적 균형을 사구 성장과 연결한다.
5. 큰 입자의 무게와 작은 이동량을 사구 형성의 어려움과 연결한다.
6. 카메라·MediaPipe·학교 네트워크에 문제가 생겨도 수동 조작으로 수업이 중단되지 않게 한다.
7. 데스크톱부터 스마트폰까지 가로 스크롤 없이 사용할 수 있게 한다.

## 5. 비목표

- 실제 풍동 실험의 절대 풍속이나 전단 속도를 정밀 계산하지 않는다.
- 실제 사구의 3차원 형태를 수치 유체역학으로 재현하지 않는다.
- 학생 얼굴·영상·손 좌표를 서버에 저장하지 않는다.
- 사용자 계정, 서버 데이터베이스, API 키를 사용하지 않는다.
- 아주 작은 입자·모래·자갈의 모든 자연환경을 일반화하지 않는다.

## 6. 사용자 유형과 사용 시나리오

### 6.1 교사

- 빔프로젝터로 웹앱을 띄우고 손으로 바람을 만든다.
- 같은 바람에서 세 입자의 이동을 비교하도록 발문한다.
- 카메라 또는 학교망 문제가 생기면 즉시 수동 조작으로 전환한다.
- 학생의 관찰 결과를 CSV로 저장한다.

### 6.2 학생

- 카메라 앞에서 손을 한쪽으로 빠르게 밀어 바람을 만든다.
- 세 입자의 이동·사구 성장·구역 밖 이동 값을 비교한다.
- 입자 크기 슬라이더로 조건을 바꾸어 최적 범위를 탐색한다.
- 관찰 결과를 기록하고 과학적 설명을 작성한다.

## 7. 기능 요구사항

### FR-01 카메라 우선 시작

- `카메라 시작` 버튼을 눌렀을 때 `getUserMedia()`를 먼저 실행한다.
- MediaPipe 모듈과 모델의 로딩은 카메라 연결 후 비동기로 진행한다.
- MediaPipe CDN이 응답하지 않아도 카메라 영상은 보여야 한다.
- 카메라 연결 상태를 `꺼짐·연결 중·연결됨·오류`로 표시한다.

### FR-02 3단계 인식 구조

1. **MediaPipe 손 인식**: 손 관절점을 이용해 손바닥 중심의 가로 속도와 방향을 계산한다.
2. **카메라 움직임 감지**: MediaPipe를 사용할 수 없을 때 저해상도 프레임 차분으로 가로 움직임을 계산한다.
3. **수동 조작**: 카메라 자체를 사용할 수 없을 때 슬라이더와 방향 버튼을 제공한다.

화면은 현재 사용 중인 인식 방식을 명확히 표시해야 한다.

### FR-03 MediaPipe 오류 복구

- 각 외부 모듈·WASM·모델 로딩에 제한 시간을 둔다.
- 1차 소스 실패 시 2차 소스를 시도한다.
- 시간 초과 또는 초기화 실패 시 움직임 감지로 자동 전환한다.
- 사용자가 `MediaPipe 다시 연결`을 누를 수 있다.
- 실패가 전체 앱의 예외로 전파되어 시뮬레이션을 중단하면 안 된다.

### FR-04 손동작-바람 매핑

- 화면에서 손이 오른쪽으로 빠르게 움직이면 오른쪽 바람을 만든다.
- 화면에서 손이 왼쪽으로 빠르게 움직이면 왼쪽 바람을 만든다.
- 손의 이동 속도가 클수록 상대 바람 세기가 커진다.
- 한 번의 동작 뒤 손을 천천히 되돌릴 때 반대 바람이 과도하게 발생하지 않도록 잠금 시간을 둔다.
- 손을 멈추면 바람이 점차 약해진다.

### FR-05 수동 조작

- 바람 세기 0~10 슬라이더
- 왼쪽·멈춤·오른쪽 버튼
- 키보드 `←`, `→`, `0`
- 수동 모드로 전환하면 카메라 스트림을 종료해 자원을 해제한다.

### FR-06 세 종류 비교

같은 바람을 다음 입자에 동시에 적용한다.

| ID | 입자 | 크기 |
|---|---|---:|
| fine | 아주 작은 입자(실트 크기) | 0.02 mm |
| sand | 사구를 잘 이루는 모래 | 0.25 mm |
| coarse | 큰 입자(잔자갈 크기) | 3.0 mm |

각 카드에 현재 움직임, 사구 성장 지수, 관찰 구역 밖 이동 지수를 표시한다.

### FR-07 한 종류 자세히

- 0.005~5 mm 로그 슬라이더 제공
- 빠른 선택 0.02 mm·0.25 mm·3.0 mm 제공
- 상세 캔버스에 사구 단면과 이동 입자 표시
- 충분히 성장하면 바람받이 사면·사구 마루·바람그늘 사면 표시

### FR-08 과학 설명

현재 입자 크기와 바람 조건에 따라 다음을 갱신한다.

- 입자 분류
- 현재 이동 상태
- 이동 상태의 원인
- 이동 시작 기준
- 사구 형성 조건 지수
- 조건에 대한 결론

학생 화면의 핵심 상태 용어는 현상 중심의 한국어 문장으로 표시한다.

### FR-09 개념 그래프

- x축: 입자 크기(mm), 로그 눈금
- y축: 이동 시작에 필요한 상대 바람 세기
- 작은 입자 쪽 응집력과 큰 입자 쪽 무게를 반영한 U자형 경향
- 현재 바람과 선택 입자를 그래프에 표시
- 실제 정량 풍속 그래프가 아니라 교육용 개념 그래프임을 명시

### FR-10 관찰 기록

- 기록 항목: 시각, 모드, 입자, 입자 크기, 상대 바람 세기, 풍향, 이동 상태, 사구 형성 가능성, 성장 지수, 구역 밖 이동 지수
- 브라우저 로컬 저장
- 최대 60개 유지
- 표에는 최근 12개 표시
- UTF-8 BOM을 포함한 CSV 저장

### FR-11 오류 가시성

- 처리되지 않은 JavaScript 오류가 발생하면 상단에 오류 배너를 표시한다.
- 오류 배너에 새로고침 버튼을 제공한다.
- 카메라 권한 거부, 카메라 없음, 카메라 점유, 보안 컨텍스트 오류를 구분해 안내한다.

### FR-12 반응형 화면

- 넓은 화면: 조작·시뮬레이션·과학 설명 3열
- 중간 화면: 조작+시뮬레이션 2열, 설명은 아래
- 좁은 화면: 모든 영역 1열
- 390×844 CSS 픽셀에서 문서 전체의 가로 오버플로가 없어야 한다.
- 모바일에서 카메라와 각 비교 캔버스의 최소 관찰 높이를 확보한다.

### FR-13 개인정보 보호

- 카메라 영상은 `<video>`와 브라우저 메모리에서만 처리한다.
- 영상·프레임·랜드마크를 네트워크로 업로드하지 않는다.
- 녹화 기능을 제공하지 않는다.
- 카메라를 끄거나 수동 모드로 전환할 때 모든 트랙을 정지한다.

## 8. 과학 모형 규칙

### 8.1 입자 구분

- 실트·점토 크기: 0.063 mm 미만
- 모래: 0.063~2.0 mm
- 큰 입자: 2.0 mm 이상

경계값은 교육용 분류를 위한 기준이며, 화면의 실제 동작은 연속 함수로 계산한다.

### 8.2 이동 시작 기준

입자 크기 `d`에 대해 상대 이동 시작 기준을 U자형 함수로 계산한다.

- `d`가 매우 작을수록 응집력 항을 추가한다.
- `d`가 0.5 mm보다 커질수록 무게 항을 추가한다.
- 결과는 상대 바람 세기 2.45~9.6 범위로 제한한다.

### 8.3 이동 상태

- 바람 < 기준, 아주 작은 입자: 입자끼리 달라붙어 표면에 남음
- 바람 ≥ 기준, 아주 작은 입자: 공중에 떠서 멀리 이동
- 바람 ≥ 기준, 0.063~0.7 mm: 튀어 오르며 이동한 뒤 다시 쌓임
- 바람 ≥ 기준, 0.7~2.0 mm: 지표면을 따라 구르거나 미끄러짐
- 바람 ≥ 기준, 2.0 mm 이상: 아주 강한 바람에 조금씩 굴러감

### 8.4 사구 형성 조건

사구 형성 지수는 다음 개념 요소를 곱해 계산한다.

- 이동 시작 여부
- 0.25 mm 부근의 입자 적합성
- 이동 후 관찰 구역에 남는 비율
- 이동 시작 기준을 초과한 정도

절대값이 아니라 조건 간 비교를 위한 상대 지수다.

## 9. 기술 구조

```text
index.html
  └─ js/app.js
      ├─ camera-controller.js
      │   ├─ MediaDevices.getUserMedia
      │   ├─ MediaPipe HandLandmarker
      │   └─ motion-analyzer.js
      ├─ dune-simulation.js
      └─ science-model.js
```

- 빌드 도구 없이 ES Modules로 실행
- GitHub Pages의 프로젝트 하위 경로에서도 작동하도록 상대 경로 사용
- CSS·JS 파일에 버전 쿼리 추가
- `.nojekyll` 포함

## 10. 실패 상황과 대응

| 실패 상황 | 대응 |
|---|---|
| CDN DNS 차단·응답 지연 | 제한 시간 후 움직임 감지로 전환 |
| MediaPipe 모듈 404 | 다음 버전 소스 시도 |
| GPU delegate 실패 | CPU delegate 재시도 |
| 모델 로딩 실패 | 움직임 감지 유지, 재연결 버튼 표시 |
| 카메라 권한 거부 | 권한 안내 + 수동 조작 |
| 카메라 장치 없음 | 장치 없음 안내 + 수동 조작 |
| 다른 앱이 카메라 사용 | 앱 종료 안내 + 수동 조작 |
| 숨겨진 캔버스 크기 0×0 | 크기 변경 무시, 보이는 순간 재계산 |
| JS 실행 오류 | 상단 오류 배너와 새로고침 버튼 |
| 브라우저 캐시 | 버전 쿼리와 강력 새로고침 안내 |

## 11. 수용 기준

### 카메라·인식

- [x] MediaPipe 외부 파일 연결 실패를 강제로 만든 상태에서도 3초 이내에 카메라 영상이 표시된다.
- [x] 같은 조건에서 인식 방식이 `움직임 감지(대체)`로 바뀌고 앱이 계속 작동한다.
- [x] 카메라 시작·끄기 버튼 상태가 올바르게 바뀐다.
- [x] 수동 모드 전환 시 카메라가 종료된다.

### 화면

- [x] 초기 비교 모드의 세 캔버스가 100×100px 이상으로 렌더링된다.
- [x] 숨겨진 상세 캔버스는 초기 0×0 레이아웃에서 강제 확대하지 않는다.
- [x] 상세 모드 전환 후 실제 표시 크기로 다시 계산된다.
- [x] 390×844 화면에서 문서 가로 오버플로가 0px이다.
- [x] 모바일 비교 카드와 캔버스가 한 열로 표시된다.

### 조작·기록

- [x] 수동 오른쪽 바람 6.0이 상태 표시와 시뮬레이션에 반영된다.
- [x] 모래 사구 성장 지수가 시간에 따라 증가한다.
- [x] 상세 모드에서 관찰 기록 버튼을 누르면 표에 한 행이 추가된다.
- [x] JavaScript 처리되지 않은 예외가 발생하지 않는다.

## 12. 향후 개선 후보

- MediaPipe 라이브러리·WASM·모델을 저장소 안에 포함한 완전 오프라인 버전
- Web Worker 또는 OffscreenCanvas로 손 인식과 시뮬레이션 분리
- 장애물·식생·수분·모래 공급량 변인 추가
- 풍속 고정 실험 시나리오와 활동지 자동 생성
- 조별 결과 비교를 위한 Google Sheets 선택적 연동
- 실제 풍동 자료와 개념 모형 비교 모드
