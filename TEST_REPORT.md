# 자가 점검 보고서

## 1. 점검 대상

- 제품: 손으로 만드는 사구 실험실
- 버전: 2.1.0
- 점검일: 2026-07-31
- 배포 형태: GitHub Pages용 정적 웹앱

## 2. 이전 버전에서 재현한 문제

### 2.1 카메라가 시작되지 않음

이전 구현은 다음 순서였습니다.

1. MediaPipe JavaScript 모듈을 외부 CDN에서 동적 import
2. WASM 준비
3. 손 인식 모델 준비
4. 이후에 `getUserMedia()`로 카메라 요청

외부 CDN의 DNS 연결을 차단한 브라우저 테스트에서 20초 이상 다음 상태가 유지되었습니다.

- 상태: `손 인식 모형을 불러오는 중…`
- video readyState: 0
- videoWidth: 0
- 카메라 권한 요청 미실행
- 네트워크 오류: jsDelivr 리소스 `ERR_NAME_NOT_RESOLVED`

따라서 카메라 장치 문제가 아니라 **MediaPipe 외부 파일이 카메라 요청을 앞에서 막는 구조**가 핵심 원인이었습니다.

### 2.2 화면 크기 계산이 불안정함

상세 관찰 영역은 초기에는 `hidden` 상태이므로 실제 레이아웃 크기가 0×0입니다. 이전 구현은 0×0 값을 최소 240×300으로 강제 변환하여 숨겨진 캔버스를 임의 크기로 먼저 만들었습니다. 모드 전환·모바일 화면에서 잘못된 크기가 남을 가능성이 있었습니다.

## 3. 수정 사항

- 카메라 스트림을 MediaPipe보다 먼저 시작
- MediaPipe를 백그라운드에서 별도 초기화
- 외부 모듈·WASM·모델 단계별 제한 시간 적용
- MediaPipe 실패 시 프레임 차분 기반 가로 움직임 감지로 자동 전환
- 카메라 제약 조건 실패 시 단순 `{ video: true }`로 한 번 더 시도
- 수동 조작을 독립적인 최종 대체 수단으로 유지
- 숨겨진 요소의 너비·높이가 40px 미만이면 캔버스 resize를 수행하지 않음
- 모드 전환 후 두 번의 `requestAnimationFrame` 뒤 보이는 캔버스를 재계산
- 3열·2열·1열 반응형 레이아웃 재구성
- 처리되지 않은 오류를 표시하는 상단 진단 배너 추가
- MediaPipe 재연결 버튼과 현재 인식 방식 표시 추가

## 4. 자동 검사 결과

### 4.1 JavaScript 구문 검사

실행 명령:

```bash
npm run check
```

검사 파일:

- `js/app.js`
- `js/camera-controller.js`
- `js/motion-analyzer.js`
- `js/dune-simulation.js`
- `js/science-model.js`

결과: **통과**

### 4.2 단위 테스트

실행 명령:

```bash
npm test
```

검사 내용:

- 입자 크기 로그 슬라이더 왕복 변환
- 아주 작은 입자·모래·큰 입자의 이동 시작 기준 경향
- 바람 조건별 이동 상태 판정
- 사구 형성 지수의 상대적 크기
- 영상 프레임 차분의 움직임 중심·방향·속도 판정

결과: **통과 — `과학 모형·영상 움직임 분석 테스트 통과`**

### 4.3 실제 브라우저 자동 점검

환경:

- Chromium 144 headless
- 1440×1200 데스크톱 화면
- 390×844 모바일 화면
- 가상 카메라 스트림
- 외부 MediaPipe 연결 실패를 의도적으로 재현

점검 결과:

| 점검 항목 | 결과 |
|---|---|
| 초기 앱 실행 상태 | 정상 |
| 상단 치명적 오류 배너 | 표시되지 않음 |
| 비교 캔버스 3개 크기 | 각 319×239px, 정상 |
| 숨겨진 상세 캔버스 | client 0×0, 임의 강제 확대 없음 |
| 외부 MediaPipe 연결 실패 중 카메라 | 640×480, readyState 4, 정상 |
| 자동 대체 인식 | `움직임 감지(대체)`로 전환 |
| 카메라 시작·끄기 버튼 | 정상 전환 |
| 수동 오른쪽 바람 | 6.0/10 적용 |
| 수동 조작 중 모래 성장 | 성장 지수 증가 확인 |
| 상세 모드 캔버스 | 985×554px로 재계산 |
| 관찰 결과 기록 | 표에 1행 추가 |
| 모바일 viewport | 390×844px |
| 모바일 문서 가로 오버플로 | 0px |
| 모바일 비교 카드 | 한 열, 각 338px |
| 처리되지 않은 JS 예외 | 0건 |
| 네트워크 외 비정상 로그 오류 | 0건 |

브라우저 자동 점검 결과: **통과**

### 4.4 MediaPipe 정상 초기화 경로 점검

외부 네트워크 상태와 무관하게 MediaPipe 성공 경로 자체를 확인하기 위해, 공식 Web API와 같은 형태의 `FilesetResolver`·`HandLandmarker` 모듈 응답을 브라우저 네트워크 계층에서 모의 제공했습니다. 모의 손 관절 좌표는 펼친 손이 화면 오른쪽으로 빠르게 이동하도록 구성했습니다.

확인 결과:

- 카메라: `연결됨`
- 인식 방식: `MediaPipe 손 인식`
- 인식 상태: `MediaPipe 손 관절 인식 작동 중`
- 모의 손 이동 후 풍향: `오른쪽 →`
- 상대 바람 세기: `9.8 / 10`
- 처리되지 않은 JavaScript 예외: 0건

MediaPipe 정상 초기화·손 좌표 처리·바람 변환 경로: **통과**

## 5. 확인 범위의 한계

테스트 환경에는 실제 교실의 물리적 웹캠과 학생 손이 없으므로, 실제 손 모양·조명·배경에 따른 MediaPipe 인식률까지 동일하게 재현하지는 못했습니다. 대신 다음을 확인했습니다.

- 가상 카메라 스트림이 실제 `<video>` 요소에 연결되는지
- 외부 MediaPipe가 실패해도 카메라와 앱이 멈추지 않는지
- 대체 움직임 감지와 수동 조작이 정상 동작하는지
- 손 관절 처리 로직과 바람 변환 함수의 구문·구조가 정상인지

최종 배포 후에는 교실에서 다음 1분 점검을 권장합니다.

1. GitHub Pages HTTPS 주소에서 카메라 권한 허용
2. 화면 상단 카메라가 `연결됨`인지 확인
3. 인식 방식이 `MediaPipe 손 인식` 또는 `움직임 감지(대체)`인지 확인
4. 손을 오른쪽으로 빠르게 밀었을 때 풍향이 `오른쪽 →`인지 확인
5. 수동 조작으로 바람 6.0을 적용했을 때 모래 카드의 성장 지수가 증가하는지 확인
